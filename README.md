# Google search automation using terminal
This simple Python Script will automate the google searches that you type in the command line. The top searches will open automatically in your default browswer.

## Python 3 required
